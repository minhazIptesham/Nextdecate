/*
import 'package:flutter/material.dart';

import 'package:get/get.dart';

class HomeMainScreenView extends StatelessWidget {
  const HomeMainScreenView({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('HomemainscreenView'),
        centerTitle: true,
      ),
      body: Center(
        child: Text(
          'HomemainscreenView is working',
          style: TextStyle(fontSize: 20),
        ),
      ),
    );
  }
}
*/
